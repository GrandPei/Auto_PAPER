现在实现最终API。

要求：

最终用户只能调用：

download_paper_pdf(title)

download_paper(title)

download_by_doi(doi)

download_by_url(url)

download_many(list[str])

所有复杂逻辑全部隐藏。

返回：

DownloadResult

增加完整文档。

增加examples。

增加pytest。

最终整个项目可以直接pip install。

输出项目结构。