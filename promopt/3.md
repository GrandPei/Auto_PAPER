现在实现Provider架构。

要求：

采用抽象工厂+策略模式。

创建：

providers/base.py

定义：

BaseProvider

必须包含：

async search()

async get_metadata()

async get_pdf_url()

async download()

统一返回Paper对象。

任何新的Provider都必须继承BaseProvider。

最后给出类图说明。